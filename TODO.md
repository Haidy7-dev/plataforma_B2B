- [ ] Revisar e implementar validación CSV ADMIN para módulo “Inventario CSV” (tabla `recurso`)
  - [ ] back/src/routes/logistics.js: permitir SOLO encabezados `nombre,tipo,stock,precio` para `/inventario/preview` y `/inventario/importar`
  - [ ] back/src/routes/logistics.js: ajustar `/inventario/plantilla` para que genere plantilla con SOLO esos campos
  - [ ] back/src/routes/logistics.js: añadir logs en consola con encabezados detectados, esperados, diferencias y motivo de rechazo
- [ ] back/src/services/recursoImportService.js: ajustar validación para NO exigir `estado` e `id_empresa` desde CSV; usar defaults del sistema
- [ ] front/src/role-pages/admin/pages/InventoryImport.jsx
  - [ ] Ajustar `requiredHeaders` para SOLO `nombre,tipo,stock,precio`
  - [ ] Mantener UI sin cambios visuales; mostrar `estado/id_empresa` como `-` si no vienen del CSV
- [ ] Probar localmente subiendo un CSV con encabezados `nombre,tipo,stock,precio` y confirmar que ya NO aparece “Faltan columnas obligatorias”
- [ ] Verificar logs de consola para confirmar:
  - Columnas encontradas en el CSV
  - Columnas obligatorias esperadas
  - Diferencias detectadas
  - Motivo exacto del rechazo si ocurre
